# Recommended start command. The script enables GPU rendering, gives Android
# enough memory, and waits for boot completion before Flutter starts.
cd "C:\Users\Ануар\Desktop\Папки\Anuar\UneBil App"
.\scripts\Start-AndroidEmulator.ps1

# Use this only once when an emulator snapshot itself is damaged:
# .\scripts\Start-AndroidEmulator.ps1 -ColdBoot

# Use this only when Android data is damaged. It resets the virtual phone:
# .\scripts\Start-AndroidEmulator.ps1 -WipeData
